<Window x:Class="LogAnalizerWpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LogAnalizerWpfClient"
        Title="Alarm Log Analyzer" Height="400" Width="600">
    <!-- Главное окно приложения -->
    <Grid Margin="10">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
            <!-- Блок импорта логов с указанием недели -->
            <TextBlock Text="Import Logs with Week label:" FontWeight="Bold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox x:Name="comboImportWeek" Width="100" Margin="0,0,10,0"
                          VerticalAlignment="Center"/>
                <Button x:Name="btnImport" Content="Import Log" Click="btnImport_Click"/> 
                <Button Content="Reset Logs" Margin="5,0,0,0" Width="80" Click="btnResetLogs_Click"/>
                        <!--VerticalAlignment="Center"/>-->
            </StackPanel>
            <!-- Список загруженных логов с указанием недели и файла -->
            <TextBlock Text="Loaded logs:" FontWeight="Bold"/>
            <ListBox x:Name="listBoxLogs" Margin="0,5,0,10" 
                     IsHitTestVisible="False" BorderThickness="0"/>
            <Separator Margin="0,5,0,10"/>
            <!-- Блок выбора двух недель для сравнения -->
            <TextBlock Text="Compare Two Weeks:" FontWeight="Bold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="comboWeek1" Width="100" Margin="0,0,10,0"
                          VerticalAlignment="Center" />
                <ComboBox x:Name="comboWeek2" Width="100" Margin="0,0,10,0"
                          VerticalAlignment="Center" />
                <Button x:Name="btnCompare" Content="Compare Logs" Click="btnCompare_Click" 
                        VerticalAlignment="Center"/>
                <Button Content="Verify Counts" Click="btnVerifyCounts_Click" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>